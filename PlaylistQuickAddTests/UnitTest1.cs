
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace PlaylistQuickAddTests
{

    // generate tests for Authorization.GetSpotifyAccessToken using Microsoft.VisualStudio.TestTools.UnitTesting
    [TestClass]
    public class AuthorizationTests
    {
        [TestMethod]
        public void TestGetSpotifyAccessToken()
        {
            Assert.Equals(true, true);
            //var authorization = new Authorization();
            //var token = authorization.GetSpotifyAccessToken().Result;
            //Assert.IsNotNull(token.AccessToken);
        }
    }

}