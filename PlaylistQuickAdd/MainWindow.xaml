<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="PlaylistQuickAdd.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PlaylistQuickAdd"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <NavigationView PaneTitle="Playlist Quickadd" IsBackButtonVisible="Collapsed">
        <NavigationView.MenuItems>
        <NavigationViewItem Content="Home" Icon="Home" />
        <NavigationViewItem Content="Playlists" Icon="MusicInfo" />
        </NavigationView.MenuItems>

    <StackPanel>
        <Grid>
            <GridView ItemsSource="{x:Bind Playlists, Mode=OneWay}">
                <GridView.ItemTemplate>
                    <DataTemplate x:DataType="local:Playlist">
                        <StackPanel Orientation="Vertical">
                            <Image Source="{x:Bind PlaylistCover}" Width="100" Height="100" />
                            <TextBlock Text="{x:Bind Title, Mode=OneWay}" />
                        </StackPanel>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>
        </Grid>
        <TextBlock x:Name="AccessTokenTextBlock" FontSize="20"/>
        <Button Content="Login" Click="LoginSpotify" />
    </StackPanel>
    </NavigationView>
    
    
</Window>
